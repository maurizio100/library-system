services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_H2_CONSOLE_SETTINGS_WEB-ALLOW-OTHERS=true

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - app
